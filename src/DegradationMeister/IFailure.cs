namespace DegradationMeister
{
    /// <summary>
    /// Interface being used to define a failure. This is just done by attributing it to the given interface
    /// </summary>
    public interface IFailure
    {
    }
}